# Sample Repo for Code Retreat

### Build the project

```console
$ dotnet build
```

### Run the tests

```console
$ dotnet test TW.Offboarding.Tests
```

### Run the application

Run the application which will be listening on port `5000`.

```console
$ dotnet run --project TW.Offboarding
```
